import {
  Loader2,
  LogIn,
  LogOut,
  Search,
  Send,
  User,
  Car,
} from "lucide-react"

export const Icons = {
  logo: Car,
  spinner: Loader2,
  search: Search,
  send: Send,
  login: LogIn,
  logout: LogOut,
  user: User,
}
